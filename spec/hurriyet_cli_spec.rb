require 'spec_helper'

describe HurriyetCli do
  it 'has a version number' do
    expect(HurriyetCli::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
